---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="ko">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			:where(html, body) { overflow-x: hidden; }
			main.page {
				width: 100%;
				max-width: 100%;
				margin: 0;
				padding: 0;
			}
			.hero {
				position: relative;
				min-height: 100svh;
				display: grid;
				place-items: center;
				place-content: center;
				padding: 6vh 1rem;
				background: url('/golf2.png') center / cover no-repeat;
				color: #fff;
				box-sizing: border-box;
			}
			.hero-inner {
				width: min(1200px, calc(100% - 2rem));
				max-width: 100%;
				padding: 2.25em 1.5em;
				text-align: center;
				border-radius: 16px;
				background: rgba(255, 255, 255, 0.08);
				box-shadow: 0 10px 30px rgba(0,0,0,0.25);
				backdrop-filter: blur(6px);
				-webkit-backdrop-filter: blur(6px);
				border: 1px solid rgba(255, 255, 255, 0.15);
				margin: 0 auto;
			}
			@media (min-width: 1200px) {
				.hero p { white-space: nowrap; display: inline-block; }
				.hero-inner { width: min(1280px, calc(100% - 2rem)); }
			}
			.hero-inner, .hero-inner p { word-break: keep-all; }
			.hero h1 {
				margin: 0 0 0.5em 0;
				font-size: clamp(1.25rem, 3vw, 1.75rem);
				letter-spacing: -0.02em;
				white-space: nowrap;
			}
			.hero p.lead {
				margin: 0.2em auto 1em auto;
				max-width: 80ch;
				font-size: clamp(0.95rem, 2.2vw, 1.0625rem);
				line-height: 1.9;
				color: rgba(255,255,255,0.95);
			}
			.hero p { margin: 0.3em 0; line-height: 1.8; }
			.hero p.lead { white-space: nowrap; display: inline-block; }
			/* 참고 이미지: 기본 비표시, 모바일에서 표시 */
			.ref-image { display: none; width: 100%; height: auto; margin-top: 0.85rem; border-radius: 12px; box-shadow: 0 8px 24px rgba(0,0,0,0.25); }
			@media (max-width: 700px) {
				.ref-image { display: block; }
			}
			@supports(padding: max(0px)) {
				.hero-inner { padding-bottom: max(1.5em, env(safe-area-inset-bottom)); }
			}
		</style>
	</head>
	<body>
		<Header />
		<main class="page">
			<section class="hero">
				<div class="hero-inner">
					<h1>골프 여행, 더 가볍고 더 여유롭게</h1>
					<p class="lead" style="max-width:80ch">필요한 것만 담아 더욱 세련된 골프 투어를 소개합니다.</p><br>
					<p>라운드 중심 일정으로 이동 피로를 줄입니다.</p><br>
					<p>취향과 실력에 맞춘 명문 코스를 선별합니다.</p><br>
					<p>전담 매니저 동행으로 예약과 이동을 모두 케어합니다.</p><br>
					<p>리조트형 동선으로 휴식과 라운드를 조화롭게.</p><br>
					<p>유연한 대체 일정으로 변수에도 안심할 수 있습니다.</p><br>
					<p>스냅 기록 서비스로 순간을 특별하게 남깁니다.</p><br>
					<p>필수 보험 포함으로 안전까지 꼼꼼하게 챙깁니다.</p><br>
					<p>간결하지만 만족스러운, 프리미엄 골프 여행.</p>
					<img class="ref-image" src="/golf2.png" alt="골프 여행 참고 이미지" loading="lazy" decoding="async" />
				</div>
			</section>

			<!-- Golf Life 확장 본문(상단 메뉴 제거 버전) -->
			<section class="glife" aria-label="Golf Life 확장 콘텐츠">
				<div class="glife-container">
					<section class="gl-hero gl-reveal">
						<div>
							<div class="gl-badge gl-pulse">💚 평생 골프 프로젝트</div>
							<h2 class="gl-h1">오늘도 내일도,<br> <strong>마지막 퍼팅까지!</strong> ⛳️</h2>
							<p class="gl-sub">
								“일하고 쉬고 또 골프!”<br>
								페어웨이에서 해 뜨고 지는 날까지, 위트와 여유로 채우는 <b>골프 라이프</b>를 소개합니다.
							</p>
							<div class="gl-cta">
								<a class="gl-btn gl-primary" href="#gl-join">라운드 예약하기 🏌️‍♂️</a>
								<a class="gl-btn gl-ghost" href="#gl-features">분위기 구경하기 🌤️</a>
							</div>

							<!-- 마퀴는 전체 컨테이너 폭과 일치하도록 gl-hero 밖(그리드 단위)으로 이동 -->

						</div>
					</section>

					<!-- gl-why: 컨테이너 전체 폭으로 확장 -->
					<section class="gl-card gl-reveal" style="margin-top:8px">
						<div id="gl-why" class="gl-grid">
							<div class="gl-card gl-feature gl-reveal">
								<div>🌞</div>
								<h3>해 뜨면 티샷</h3>
								<p class="gl-muted">아침 햇살에 비타민D 충전. “내 비거리도 10야드 충전된 느낌!”</p>
							</div>
							<div class="gl-card gl-feature gl-reveal">
								<div>🫶</div>
								<h3>친구는 페어웨이에서</h3>
								<p class="gl-muted">버디보다 값진 건 ‘나이스!’ 한마디. 칭찬이 곧 멘탈 케어.</p>
							</div>
							<div class="gl-card gl-feature gl-reveal">
								<div>📈</div>
								<h3>매일 1% 상향</h3>
								<p class="gl-muted">루틴·템포·마인드셋, 소소하게 쌓이면 핸디는 내려갑니다.</p>
							</div>
						</div>
					</section>

					<!-- 컨테이너 폭과 동일하게 펼쳐지는 마퀴 -->
					<div class="gl-marquee gl-float gl-span" aria-label="골프 라이프 티커">
						<div class="gl-track">
							<span>✨ “드라이버는 우정, 퍼팅은 사랑, 벙커는 인내.”  |  🧃 19홀에서는 칭찬만 가능  |  📒 스코어는 기록하고, 추억은 저장!</span>
							<span aria-hidden="true">✨ “드라이버는 우정, 퍼팅은 사랑, 벙커는 인내.”  |  🧃 19홀에서는 칭찬만 가능  |  📒 스코어는 기록하고, 추억은 저장!</span>
						</div>
					</div>

					<section id="gl-features" class="gl-card gl-reveal" style="margin-top:8px">
						<div class="gl-row">
							<div class="gl-pill">☕️ 새벽 티타임</div>
							<div class="gl-pill">🎶 버디 브금</div>
							<div class="gl-pill">🍀 멘탈 세이프존</div>
							<div class="gl-pill">🧑‍🍳 19홀 브런치</div>
						</div>
						<p class="gl-quote" style="margin-top:12px">
							“골프는 인생의 축소판. 가끔 러프에 들어가도, 웃으며 레이업하면 다시 페어웨이!” 😄
						</p>
					</section>

					<section id="gl-join" class="gl-card gl-reveal" style="margin-top:18px">
						<h3 style="margin:0 0 8px">함께 칠까요? 🎯</h3>
						<p class="gl-sub">초보도, 싱글도, ‘천재 백돌이’도 환영합니다. 드레스코드는 웃음! 😆</p>
						<form onsubmit="event.preventDefault(); alert('환영해요! 라운드 소식 보내드릴게요 ⛳️');">
							<div class="gl-formgrid">
								<input aria-label="이메일" type="email" placeholder="이메일 입력" required class="gl-input">
								<button class="gl-btn gl-primary" type="submit">소식 받기 📬</button>
							</div>
						</form>
					</section>

					<section class="gl-mini-footer gl-reveal">
						⛳️ Golf Life — Fairway, Friends & Fun.<br>
						<small>※ 유머는 진지하게, 플레이는 즐겁게! 건강과 안전을 항상 우선해요.</small>
					</section>
				</div>
			</section>
		</main>

		<!-- 스크롤 리빌 스크립트 -->
		<script>
		(function(){
			const revealItems = document.querySelectorAll('.gl-reveal');
			const io = new IntersectionObserver((entries)=>{
				entries.forEach(e=>{ if(e.isIntersecting){ e.target.classList.add('is-visible'); }});
			},{ threshold: 0.15 });
			revealItems.forEach(n=> io.observe(n));
		})();
		</script>

		<!-- Golf Life 전용 스타일(네임스페이스 적용) -->
		<style>
			.glife{ padding: 24px 16px 12px 16px; background:
				radial-gradient(1200px 600px at 10% -10%, #eaf6ff, transparent 50%),
				radial-gradient(1000px 600px at 100% 0%, #f0fbf7, transparent 60%),
				#ffffff; }
			.glife-container{ max-width:1100px; margin:0 auto; padding-left: 0; padding-right: 0; }
			.gl-hero{ display:grid; grid-template-columns:1.15fr .85fr; gap:28px; align-items:center; padding: 20px 0; }
			.gl-badge{ display:inline-flex; align-items:center; gap:8px; background:linear-gradient(90deg,#10b981,#38bdf8); color:#fff; border-radius:999px; padding:8px 14px; font-weight:700; box-shadow:0 8px 24px #0000001a }
			.gl-h1{ font-size: clamp(32px, 5vw, 48px); line-height:1.08; margin:16px 0 12px }
			.gl-sub{ color:#64748b; font-size: clamp(16px, 2.2vw, 18px) }
			.gl-cta{ display:flex; gap:12px; margin-top:18px; flex-wrap:wrap }
			.gl-btn{ padding:14px 18px; border-radius:14px; border:0; font-weight:800; cursor:pointer }
			.gl-primary{ background:#10b981; color:#fff; box-shadow:0 10px 20px #10b98140 }
			.gl-ghost{ background:#fff; color:#0f172a; border:2px solid #e2e8f0 }
			.gl-card{ background:#ffffffcc; backdrop-filter:saturate(140%) blur(6px); border-radius:20px; padding:20px; box-shadow:0 10px 30px #00000010 }
			.gl-grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:18px; margin-top:26px }
			.gl-feature h3{ margin:8px 0 6px }
			.gl-illust{ aspect-ratio:1.1/1; border-radius:24px; background:linear-gradient(160deg,#fff, #f7fffb); border:1px solid #e2e8f0; box-shadow:0 20px 60px #00000012; display:flex; align-items:center; justify-content:center; position:relative; overflow:hidden }
			.gl-illust:before{ content:""; position:absolute; inset:-30% -20% auto auto; height:70%; width:70%; background:radial-gradient(closest-side,#c7f9e5 0,#c7f9e500 70%); filter:blur(30px); transform:rotate(15deg) }
			.gl-golfman{ font-size: clamp(72px, 10vw, 96px); transform:translateY(-6px) }
			.gl-marquee{ position:relative; overflow:hidden; border-radius:14px; border:1px dashed #d1fae5; background:#f0fdf4; color:#065f46; padding:10px; margin-top:18px; font-weight:700 }
			.gl-track{ display:inline-flex; align-items:center; gap:2rem; width:max-content; will-change:transform; animation: gl-marquee-x 22s linear infinite }
			.gl-track span{ flex:none; white-space:nowrap; padding-inline:8px }
			@keyframes gl-marquee-x { from { transform: translateX(0) } to { transform: translateX(-50%) } }
			@media (max-width:960px){ .gl-track{ animation-duration:28s; gap:1.25rem } }
			@media (prefers-reduced-motion: reduce){ .gl-track{ animation:none } }
			.gl-row{ display:flex; gap:14px; align-items:center; flex-wrap:wrap }
			.gl-quote{ font-size: clamp(16px, 2.2vw, 18px) }
			.gl-pill{ padding:8px 12px; border-radius:999px; background:#fff; border:1px solid #e2e8f0 }
			.gl-formgrid{ display:grid; grid-template-columns:2fr 1fr; gap:10px; margin-top:14px }
			.gl-input{ padding:14px 16px; border:1.5px solid #dbeafe; border-radius:14px; font-size:16px }
			.gl-mini-footer{ margin-top:32px; padding: 12px 0 6px; color:#64748b; text-align:center; font-size:14px }
			@media (max-width:960px){
				.gl-hero{ grid-template-columns:1fr; gap:18px }
				.gl-grid{ grid-template-columns:1fr }
			}
			/* micro-animations */
			.gl-float{ animation:gl-float 5s ease-in-out infinite }
			@keyframes gl-float{0%,100%{transform:translateY(0)}50%{transform:translateY(-6px)}}
			.gl-pulse{ animation:gl-pulse 2.2s ease-in-out infinite }
			@keyframes gl-pulse{0%,100%{box-shadow:0 0 0 0 #10b98140}50%{box-shadow:0 0 0 10px #10b98110}}
			/* reveal */
			.gl-reveal{ opacity:0; transform: translateY(12px); transition: opacity .5s ease, transform .5s ease }
			.gl-reveal.is-visible{ opacity:1; transform: translateY(0) }
			/* span helper: 컨테이너 폭 정렬 */
			.gl-span{ width:100%; }
		</style>

		<Footer />
	</body>
</html>
